{
	"version": 2,
	"name": "blog REST api",
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node",
			"config": {
				"NODE_VERSION": "14",
				"PRISMA_GENERATE": "true"
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/server.js"
		}
	]
}

